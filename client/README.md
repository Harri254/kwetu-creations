## Run Locally

**Prerequisites:** Node.js

### Frontend

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

### Backend assets

Firebase rules and schema blueprints now live in `../server/`.
